[build]
  publish = "public"
  command = "npm run lint && chmod +x ./bin/build.sh && ./bin/build.sh"

[build.environment]
  HUGO_VERSION = "0.96.0"
  HUGO_ENV = "production"
  HUGO_ENABLEGITINFO = "true"
